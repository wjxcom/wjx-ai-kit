# 变更记录

## AI 主页接口集成

- SDK、CLI 和 MCP Server 新增 `A1000107` 创建 AI 主页与 `A1000108` 更新 AI 主页能力。
- 更新接口只接受传统数字 `vid`；已发布主页需调用者先显式暂停，工具不会自动改变发布状态。
- 补充参数校验、请求预览、错误处理、测试和接口参考文档。

## 文档更新

- 首页按用户目标分流，提供 CLI、MCP 和 SDK 的清晰入口。
- JSONL/JSON 成为创建问卷的推荐方式，DSL 保留为兼容入口。
- 补充题型、MCP 工具、认证、HTTP 部署和迁移说明。
- 标明已过时的用户体系能力和冻结的 MCP npm 发布方式。
